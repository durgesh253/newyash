# Copy this content to your .env file in the backend directory

# Mailtrap SMTP Configuration
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=a91d6d790792ba
SMTP_PASSWORD=****4a03
SMTP_FROM_EMAIL=demo@leadreachai.com
SMTP_FROM_NAME=LeadReachAi Demo

# Retell AI Configuration
RETELL_API_KEY=your_retell_api_key_here
RETELL_AGENT_ID=your_retell_agent_id_here
RETELL_CONVERSATION_FLOW_ID=your_conversation_flow_id_here

# Database Configuration (if using)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=leadreachai
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Server Configuration
PORT=3003
NODE_ENV=development

# JWT Secret (for authentication)
JWT_SECRET=your_jwt_secret_key_here

# Other API Keys (if needed)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# Shopify Configuration (if using)
SHOPIFY_SHOP_NAME=your_shop_name
SHOPIFY_ACCESS_TOKEN=your_shopify_access_token 