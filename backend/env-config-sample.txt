# Production Environment Variables for LeadReach AI Backend
# Copy this to .env file and update with your actual values

# Server Configuration
PORT=8080
NODE_ENV=production

# CORS Origins (comma-separated)
CORS_ORIGINS=https://lead-reach-ai.vercel.app,https://leadreachai-2.onrender.com,http://localhost:3000,http://localhost:5173

# OpenPhone API Configuration
OPENPHONE_API_KEY=your_openphone_api_key_here
DEFAULT_OPENPHONE_NUMBER=+919054760377

# Database Configuration (if using database)
# DB_HOST=your_db_host
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=your_db_name

# Other API Keys (if needed)
# RETELL_API_KEY=your_retell_api_key
# TWILIO_ACCOUNT_SID=your_twilio_sid
# TWILIO_AUTH_TOKEN=your_twilio_token

# Email Configuration (if using email service)
# SMTP_HOST=your_smtp_host
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password

# Mailtrap SMTP Configuration
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=a91d6d790792ba
SMTP_PASSWORD=****4a03
SMTP_FROM_EMAIL=demo@leadreachai.com
SMTP_FROM_NAME=LeadReachAi Demo

# Retell AI Configuration
RETELL_API_KEY=your_retell_api_key
RETELL_AGENT_ID=your_retell_agent_id_here
RETELL_CONVERSATION_FLOW_ID=your_conversation_flow_id_here

# Database Configuration (if using)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=leadreachai
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Server Configuration
PORT=3003
NODE_ENV=development

# JWT Secret (for authentication)
JWT_SECRET=your_jwt_secret_key_here

# Other API Keys (if needed)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# Shopify Configuration (if using)
SHOPIFY_SHOP_NAME=your_shop_name
SHOPIFY_ACCESS_TOKEN=your_shopify_access_token 